# شروع کار در Claude Code دسکتاپ — یادداشت انتقال

این فایل برای این نوشته شده که وقتی پروژه رو توی Claude Code دسکتاپ باز می‌کنی،
سشن جدید بدون این‌که چیزی از قلم بیفته یا دوباره از صفر بفهمه، ادامه بده.
بعد از این‌که مطالعه‌اش کردی می‌تونی حذفش کنی یا نگه داری، به کدبیس آسیبی نمی‌زنه.

## وضعیت فعلی — کار باز/نیمه‌کاره نداریم

تمام کارهایی که تا امروز خواسته شده بود (بازنویسی کامل UI/UX، رفع ۱۶ باگ اولیه،
ممیزی کامل بک‌اند/فرانت/کارایی، گزارش‌های تحلیلی HR، و آخرین دور رفع باگ روی
پرونده‌های «در حال اجرا» و «برگشتی») **انجام و merge شده روی `main`**. هیچ
تسک نیمه‌کاره یا شاخهٔ بازِ منتظر ادامه نداریم. `git status` روی چک‌اوت فعلی هم
تمیزه (چیزی commit‌نشده باقی نمونده).

اگه یه سشن Claude Code دسکتاپ باز می‌کنی، اولین کار همیشه:

```bash
git checkout main
git pull origin main
```

⚠️ **نکته مهم:** چند سشن/ابزار مختلف (هم Claude، هم یه ابزار دیگه به اسم Cursor)
دارن موازی روی این ریپو کار می‌کنن و PR می‌زنن. یعنی احتمال داره `main` بین
جلسات تو تغییر کرده باشه حتی بدون این‌که خودت درخواستش داده باشی. همیشه قبل
از شروع کار `git pull` بزن و اگه چیزی غیرمنتظره توی تاریخچه دیدی، از خود
گیت‌هاب (commit history / PR ها) چک کن که چی اضافه شده، بعد ادامه بده.

## این پروژه چیه (خلاصهٔ خیلی کوتاه)

**DbsPulse** — سامانهٔ ارزیابی عملکرد سازمانی، فارسی/RTL کامل. زنجیرهٔ تأیید
۴مرحله‌ای: مسئول واحد → منابع انسانی → معاونت → مدیرعامل (+ یک مسیر ویژه برای
پرسنل «مدیر» که معاونت خودش نمره‌دهندهٔ اول است). مستندات کامل کسب‌وکار توی
[`پرامپت_ساخت_کلود_کد.md`](./پرامپت_ساخت_کلود_کد.md) و راه‌اندازی کامل توی
[`README.md`](./README.md) هست — این فایل جایگزین اون‌ها نیست، فقط زمینهٔ
سشن‌های اخیر رو می‌گه.

پشته: FastAPI + SQLAlchemy 2.0 + Alembic + PostgreSQL 16 (بک‌اند) /
React 19 + TS + Vite + Tailwind v4 + motion/react (Framer Motion) + Recharts
+ React Query (فرانت‌اند).

## راه‌اندازی سریع محیط توسعه

اگه محیط از قبل راه‌اندازی نشده (پایگاه‌داده/`.env` نداری):

```bash
# بک‌اند
cd backend
python3 -m venv .venv
source .venv/bin/activate          # ویندوز: .venv\Scripts\activate
pip install -r requirements.txt -r requirements-dev.txt
cp .env.example .env
# یک دیتابیس Postgres با کاربر/پسورد dbspulse/dbspulse_dev_password بساز
# (یا docker compose up -d db طبق README) و یک دیتابیس dbspulse_test هم برای تست
alembic upgrade head                # اجرای مایگریشن‌ها + seed data
uvicorn app.main:app --reload --port 8000

# فرانت‌اند (ترمینال جدا)
cd frontend
npm install
npm run dev                        # پورت 5173
```

جزئیات کامل (Docker، متغیرهای محیطی، troubleshooting ویندوز) توی
`README.md` و `setup_and_run.bat` (اسکریپت راه‌اندازی خودکار ویندوز) هست.

**کاربرهای نمونه برای تست** (پسورد همه: `DbsPulse@12345`) — جدول کامل توی
README، خلاصه‌اش:

| نام کاربری | نقش |
|---|---|
| `hr1` | منابع انسانی |
| `sup1` / `sup2` | مسئول واحد |
| `dep1` | معاونت |
| `ceo1` | مدیرعامل |

## قبل از هر commit/push

```bash
# بک‌اند
cd backend && source .venv/bin/activate
pytest -q                          # باید کامل پاس بشه (الان ~۱۷۱ تست)
ruff check app tests               # باید تمیز باشه

# فرانت‌اند
cd frontend
npm run build                      # tsc -b — سخت‌گیرتر از tsc --noEmit، همینو معیار typecheck بگیر
npx vitest run
npm run lint                       # oxlint — باید صفر error باشه (چندتا warning قدیمیِ بی‌ضرر هست)
```

اگه تغییر روی مدل دیتابیس دادی، حتماً alembic migration جدید بساز و
`alembic upgrade head` رو تست کن (رفت و برگشت — downgrade/upgrade — رو هم
چک کن که تمیز باشه).

## نکات فنی مهمی که در جریان کار به دستشون رسیدیم

- **RTL و Recharts:** چون `<html dir="rtl">` هست، هر `LabelList` با
  `position="right"` توی نمودارهای Recharts رو باید صراحتاً
  `style={{..., direction: "ltr"}}` بدی، وگرنه برچسب عدد داخل خودِ میله
  گم می‌شه (این باگ یک‌بار پیش اومد و رفع شد — الگوش رو توی
  `ReportsSection.tsx` و `DashboardPage.tsx` ببین اگه نمودار میله‌ای افقی
  جدید اضافه می‌کنی).
- **`was_returned` (نشان «برگشتی»):** این فلگ باید هم توی پاسخ فهرستی
  (`GET /evaluations`) هم توی تک‌رکورد (`GET /evaluations/{id}` و پاسخ همهٔ
  اکشن‌های گردش‌کار) محاسبه بشه — یه هلپر مشترک `_was_returned`/`_to_read`/
  `_to_detail` توی `backend/app/api/routers/evaluations.py` هست، هر
  endpoint جدیدی که EvaluationRead/Detail برمی‌گردونه باید از همینا استفاده
  کنه، نه این‌که مستقیم آبجکت ORM رو برگردونه (وگرنه `was_returned` همیشه
  `False` پیش‌فرض پیدانتیک می‌مونه — دقیقاً همین باگ یک‌بار اتفاق افتاد).
- **PDF (WeasyPrint):** روی ویندوز بدون Docker/WSL کتابخانه‌های بومی
  GTK/Pango نصب نمی‌شن و چاپ PDF کار نمی‌کنه (بقیهٔ برنامه سالمه). بخش
  «چاپ PDF» توی README رو ببین.
- **`npm run build` رو معیار typecheck بگیر، نه `tsc --noEmit`** — چون
  `noUncheckedIndexedAccess` و بقیهٔ سخت‌گیری‌ها فقط توی build واقعی
  می‌افته.
- **دسترسی/سشن:** هر تغییر روی رمز کاربر همهٔ توکن‌های قبلی‌ش رو باطل
  می‌کنه؛ اگه توی توسعه یهو ۴۰۱ خوردی، احتمالاً رمز seed یا token_version
  عوض شده — دوباره لاگین کن.

## فیچرهای عمداً غیرفعال/خارج از محدوده (فعلاً)

- **دوره‌های ارزیابی (Evaluation Periods):** عمداً غیرفعاله
  (`FEATURE_PERIODS_ENABLED = false` توی `frontend/src/appInfo.ts`). کد
  کاملشه، فقط از UI/routing مخفیه. اگه خواستی برگردونیش، همون یک خط رو
  `true` کن.
- طبق README: اتصال خودکار دسترسی از چارت سازمانی/SSO، ارزیابی ۳۶۰ درجه،
  و اعلان ایمیل/پیامک/بات — عمداً پیاده‌سازی نشدن (خارج از محدودهٔ فعلی).

## اگه دنبال کار بعدی می‌گردی

هیچ درخواست بازی نداریم، ولی اگه صاحب پروژه (شما) خواست ادامه بدید، این‌ها
جاهایی‌ان که طبیعتاً می‌تونن فاز بعدی باشن (نه درخواست‌شده، فقط پیشنهاد بر
اساس چیزی که توی کدبیس دیدم):

- فیلتر «وضعیت پرسنل / وضعیت کاربر» روی صفحهٔ کاربران هنوز جای رشد داره
  (الان فقط نقش فیلتر می‌شه).
- باندل فرانت هنوز حدود ۴۶۰KB اولیه‌ست (بعد از code-splitting) — اگه لازم
  شد بازم سبک‌تر بشه، `react-dom`/`recharts` رو می‌شه دقیق‌تر chunk کرد.
- تست‌های E2E (Playwright) نداریم؛ تا الان تست دستی+API scripting برای
  QA کافی بوده ولی برای رگرسیون بلندمدت جای بحثه.

---
این فایل رو دستی نوشتم بر اساس چیزی که توی این چند جلسه انجام دادیم؛ اگه
جایی حس کردی با کد واقعی نمی‌خونه، کد رو معیار بگیر نه این فایل رو.
